import { FilterPipeRequestGroupPipe } from './filter-pipe-request-group.pipe';

describe('FilterPipeRequestGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeRequestGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
