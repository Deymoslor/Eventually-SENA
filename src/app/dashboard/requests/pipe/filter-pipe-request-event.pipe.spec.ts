import { FilterPipeRequestEventPipe } from './filter-pipe-request-event.pipe';

describe('FilterPipeRequestEventPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeRequestEventPipe();
    expect(pipe).toBeTruthy();
  });
});
