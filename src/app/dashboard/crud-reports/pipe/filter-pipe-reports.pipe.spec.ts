import { FilterPipeReportsPipe } from './filter-pipe-reports.pipe';

describe('FilterPipeReportsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeReportsPipe();
    expect(pipe).toBeTruthy();
  });
});
