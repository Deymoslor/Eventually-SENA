import { FilterPipeEventsPipe } from './filter-pipe-events.pipe';

describe('FilterPipeEventsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeEventsPipe();
    expect(pipe).toBeTruthy();
  });
});
