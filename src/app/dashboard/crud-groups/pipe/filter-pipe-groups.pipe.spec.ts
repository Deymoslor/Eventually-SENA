import { FilterPipeGroupsPipe } from './filter-pipe-groups.pipe';

describe('FilterPipeGroupsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeGroupsPipe();
    expect(pipe).toBeTruthy();
  });
});
