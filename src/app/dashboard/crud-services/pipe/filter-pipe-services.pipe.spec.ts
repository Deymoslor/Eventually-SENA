import { FilterPipeServicesPipe } from './filter-pipe-services.pipe';

describe('FilterPipeServicesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeServicesPipe();
    expect(pipe).toBeTruthy();
  });
});
