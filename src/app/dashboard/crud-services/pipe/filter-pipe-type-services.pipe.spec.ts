import { FilterPipeTypeServicesPipe } from './filter-pipe-type-services.pipe';

describe('FilterPipeTypeServicesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeTypeServicesPipe();
    expect(pipe).toBeTruthy();
  });
});
