import { FilterPipeLikesPipe } from './filter-pipe-likes.pipe';

describe('FilterPipeLikesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeLikesPipe();
    expect(pipe).toBeTruthy();
  });
});
