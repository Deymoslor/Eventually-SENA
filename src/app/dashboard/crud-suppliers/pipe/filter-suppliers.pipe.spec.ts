import { FilterSuppliersPipe } from './filter-suppliers.pipe';

describe('FilterSuppliersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSuppliersPipe();
    expect(pipe).toBeTruthy();
  });
});
