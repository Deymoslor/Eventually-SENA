import { FilterPipeUsersPipe } from './filter-pipe-users.pipe';

describe('FilterPipeUsersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeUsersPipe();
    expect(pipe).toBeTruthy();
  });
});
